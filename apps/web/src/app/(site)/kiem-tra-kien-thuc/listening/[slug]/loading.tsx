import { ListeningTestSkeleton } from "../../../../../features/practice/ui/Skeletons";

/** Shown while the test and its audio list are fetched. */
export default function Loading() {
  return <ListeningTestSkeleton />;
}
