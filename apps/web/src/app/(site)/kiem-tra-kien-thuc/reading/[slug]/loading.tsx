import { ReadingTestSkeleton } from "../../../../../features/practice/ui/Skeletons";

/** Shown while the passage and questions are fetched. */
export default function Loading() {
  return <ReadingTestSkeleton />;
}
