import { CatalogSkeleton } from "../../../../features/practice/ui/Skeletons";

/** Shown while listReadingTests() runs on the server. */
export default function Loading() {
  return <CatalogSkeleton />;
}
