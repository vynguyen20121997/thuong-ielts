import { WritingCatalogSkeleton } from "../../../../features/practice/ui/Skeletons";

/** Hiện trong lúc `listWritingPrompts()` chạy trên server. */
export default function Loading() {
  return <WritingCatalogSkeleton />;
}
